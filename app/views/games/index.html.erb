<h1 class="pt-5">Select a game</h1>


<div class="row justify-content-center pt-5">
  <% @games.each do |game| %>
    <%= link_to game.name.upcase, game_path(game), class: "col-4" %>
  <% end %>
</div>
